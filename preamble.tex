
% Fonts
% --------------------------------------------------------------------------------
\usepackage{iftex}
\iftutex
  \usepackage{fontspec}
  \usepackage{unicode-math}
  \setmainfont{STIX Two Text}
  \setmathfont{STIX Two Math}
  \setsansfont{FiraSans}[Scale=MatchLowercase]
  \setmonofont{FiraMono}[Scale=MatchLowercase]
\else
  \usepackage{kpfonts}
  \usepackage{FiraSans}
  \usepackage[scale=0.87]{FiraMono}
% \usepackage{newtxtext,newtxmath}
\fi


% Add \usepackage{upquote} to the preamble preferably after any packages that
% change fonts or font encoding)
\usepackage{upquote}


% make section headings \sffamily
\usepackage[sf,bf,pagestyles]{titlesec}
% make headers \sffamily
\renewpagestyle{plain}[\sffamily]{
  \setfoot{}{\thepage}{}
}
\pagestyle{plain}

% make titling elements \sffamily
\usepackage{titling}
\pretitle{\begin{center}\sffamily\bfseries\huge}%
\posttitle{\par\end{center}\vskip 0.5em}
\preauthor{\begin{center}%
    \Large\sffamily \lineskip 0.5em%
    \begin{tabular}[t]{c}}%
\postauthor{\end{tabular}\par\end{center}}
\predate{\begin{center}\sffamily\large}
\postdate{\par\end{center}}

% make abstract title \sffamily
\usepackage{abstract}
\renewcommand\abstractnamefont{\sffamily}

\usepackage{microtype}

% Geometry
% --------------------------------------------------------------------------------
\usepackage{parskip}
\usepackage[scale={0.72, 0.8}]{geometry}


% Lists
% --------------------------------------------------------------------------------
\usepackage[shortlabels]{enumitem}
\setlist{noitemsep, listparindent=1em}

% Tables
% --------------------------------------------------------------------------------
\usepackage{array}
\usepackage{booktabs}

% Floats
% --------------------------------------------------------------------------------
\usepackage{float}
\usepackage{caption}
\captionsetup[figure]{format = hang, labelfont = {sf,bf},
  textfont = {it}, justification = RaggedRight}
\captionsetup[table]{format = hang, labelfont = {sf,bf},
  textfont = {it}, justification = RaggedRight}

% Color
% --------------------------------------------------------------------------------
\usepackage{xcolor}
\usepackage{tcolorbox}

% Math
% --------------------------------------------------------------------------------
\usepackage{bm}
\usepackage[fleqn,tbtags]{mathtools}
\mathtoolsset{mathic}

\usepackage{xspace}
\newcommand*{\Var}[1]{\ensuremath{\mathit{#1}}\xspace}


% Units
% --------------------------------------------------------------------------------
\usepackage{siunitx}
\newcommand*{\pct}[1]{\SI{#1}{\percent}}

\makeatletter
\DeclareSIUnit{\@EUR}{\text{\texteuro}}
\DeclareSIUnit{\@UNIT}{\text{u}}
\DeclareSIUnit\litre{l}


\sisetup{%
  exponent-product = \ensuremath{\cdot},%
  output-decimal-marker = {,},%
  group-digits = integer,%
  group-minimum-digits = 4,%
  group-separator = {{.}},%
  per-mode = symbol,%
  inter-unit-product = \,%
}%

\newcommand*{\money}[1]{\SI{#1}{\@EUR}}
\newcommand*{\price}[2][@UNIT]{\SI{#2}{\@EUR\per\csname #1\endcsname}}
\newcommand*{\wage}[2][hour]{\SI{#2}{\@EUR\per\csname #1\endcsname}}
\newcommand*{\units}[2][@UNIT]{\SI{#2}{\csname #1\endcsname}}
\makeatother


% Plots
% --------------------------------------------------------------------------------
\usepackage{pgfplots}
\pgfplotsset{compat = 1.18}
\usepgfplotslibrary{fillbetween}

% Hyperlinks
% --------------------------------------------------------------------------------
\hypersetup{colorlinks = true, linkcolor = red!60!black, citecolor = .}
\usepackage{bookmark}
\usepackage{xurl}
\urlstyle{tt}

\endinput

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: t
%%% eval: (flyspell-mode -1)
%%% End:
